require "spec_helper"

describe MvDistrictSurveyResponse do
  describe 'responses' do
    it 'should find the id for the survey name' do
      # the_survey = MvDistrictSurvey.new
      # the_survey.id = 1
      # MvDistrictSurvey.stub(:find_by_name).and_return(the_survey)
      # 
      # body = {:username=>"Camfedtest@gmail.com", :accesstoken=>"YUc8UfyeOm3W9GqNSJYs"}
      # headers = {"Content-Type"=>"application/x-www-form-urlencoded"}
      # Survey.should_receive(:post).with("/api/surveys", :body => body, :headers => headers)
      # Survey.all
      # 
      # MvDistrictSurvey.responses
    end
  end
end